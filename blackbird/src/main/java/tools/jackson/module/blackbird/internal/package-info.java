/**
 * Internal supertypes of Blackbird's generated hidden classes. This package
 * is exported only because a hidden codec defined in a user module's context
 * must resolve its supertype from there; it is internal API with no
 * compatibility guarantees, and user code must not extend or call these
 * types.
 */
package tools.jackson.module.blackbird.internal;
